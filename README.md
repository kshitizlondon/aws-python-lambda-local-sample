AWS Lambda Function - Local Development Env

- using package named "python lambda local"

- to run this in pycharm install "run bash" plugin.


useful commands::

```
$ virtualenv venv
$ . venv/bin/activate
$ pip install -r requirements.txt
```
